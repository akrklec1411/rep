import java.util.*;

class LabTask {
	
    public static int count(Map<Integer, Integer> map, Set<Integer> set) {
    	int cnt = 0;
        for (Integer i : set) {
        	for (Integer j : map.values()) {
        		if (i.equals(j)) {
        			++cnt;
        			break;
        		}
        		

        	}
        }
        return cnt;
    }
}
